---
import { cn } from "../../lib/utils";
import GradientText from "../atoms/GradientText.astro";
import SmartImage from "../atoms/SmartImage.astro";

// Images from user request (Unsplash)
// In production these should be ILSA assets, but keeping design fidelity for now.
const images = [
    "https://images.unsplash.com/photo-1719368472026-dc26f70a9b76?q=80&h=800&w=800&auto=format&fit=crop",
    "https://images.unsplash.com/photo-1649265825072-f7dd6942baed?q=80&h=800&w=800&auto=format&fit=crop",
    "https://images.unsplash.com/photo-1555212697-194d092e3b8f?q=80&h=800&w=800&auto=format&fit=crop",
    "https://images.unsplash.com/photo-1729086046027-09979ade13fd?q=80&h=800&w=800&auto=format&fit=crop",
    "https://images.unsplash.com/photo-1601568494843-772eb04aca5d?q=80&h=800&w=800&auto=format&fit=crop",
    "https://images.unsplash.com/photo-1585687501004-615dfdfde7f1?q=80&h=800&w=800&auto=format&fit=crop"
];

interface Props {
    class?: string;
}

const { class: className } = Astro.props;
---

<section class={cn("w-full py-8 md:py-16", className)}>
    <div class="max-w-[1400px] mx-auto px-6">
        <div class="text-center mb-10">
            <GradientText 
                text="Galería Multimedia" 
                size="text-3xl md:text-5xl" 
                weight="font-bold"
                from="var(--ilsa-blue)" 
                to="var(--ilsa-mujeres)"
                class="justify-center mb-4"
            />
            <p class="text-sm md:text-lg text-slate-500 max-w-2xl mx-auto text-balance">
                Una colección visual de nuestras acciones recientes. Cada imagen cuenta una historia de territorio, derechos y resistencia.
            </p>
        </div>

        <div 
            class="flex flex-row md:flex-row items-center gap-4 h-[350px] md:h-[500px] w-screen relative left-[calc(-50vw+50%)] px-4 md:px-0 md:w-full md:left-auto mt-10 overflow-x-auto snap-x snap-mandatory scrollbar-hide focus-visible:ring-2 focus-visible:ring-[#4E7CCE] focus-visible:outline-none rounded-xl touch-scroll"
            tabindex="0"
            aria-label="Galería multimedia"
            role="region"
        >
            {images.map((img, index) => (
                <div class="relative group flex-shrink-0 md:flex-grow transition-all duration-500 ease-out w-[85vw] md:w-28 h-full md:h-full rounded-2xl overflow-hidden md:hover:grow-[3] shadow-lg hover:shadow-2xl border border-white/20 snap-center">
                    <SmartImage 
                        class="h-full w-full object-cover object-center transition-transform duration-700"
                        src={img}
                        alt={`Galeria ILSA ${index + 1}`}
                    />
                    <!-- Overlay Gradient on Hover -->
                    <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
                </div>
            ))}
        </div>

        <style>
            .scrollbar-hide::-webkit-scrollbar {
                display: none;
            }
            .scrollbar-hide {
                -ms-overflow-style: none;
                scrollbar-width: none;
            }
        </style>
    </div>
</section>
